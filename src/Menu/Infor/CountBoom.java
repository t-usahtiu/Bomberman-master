package Menu.Infor;

import Constant.Sprite;

public class CountBoom extends Infor {
    public CountBoom(String text, int x, int y) {
        super(text, x, y);
        this.image = Sprite.bombAnim[0];
    }
}
